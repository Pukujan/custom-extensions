# Image prompt record

This preview pins `content-generation-modules@0.1.2`. Narrative raster assets use a short exact title and subtitle; small helper graphics remain text-free.

## Hero

- Title: `Small extensions. Clear boundaries.`
- Subtitle: `Install only what you need, with evidence you can inspect.`
- Scene: one person, one friendly browser assistant, and three bounded cards for clean-up, export, and verify.
- Role: wide `1672x941`; left copy panel; subjects on the right.

## Supporting square

- Title: `Local by default`
- Subtitle: `Preview, export, or change data only when the contract allows it.`
- Scene: three separate utility cards with restrained symbols and no extra labels.
- Role: square `1254x1254`; explain local-first scope, not a second hero.

The image provider does not expose a stable seed here, so the reproducible record is the exact copy, scene, composition, palette, reference style, dimensions, and rejection criteria.
